---
- name: Copy artifact
  become: true
  copy:
    src: artifact.tar.gz
    dest: /home/ubuntu/artifact.tar.gz

- name: Start app
  become: true
  shell: |
    cd /home/ubuntu/
    tar xzvf artifact.tar.gz
    cd backend
    npm install
    npm run build
    pm2 start npm -- start
